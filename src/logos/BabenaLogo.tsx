import styled from "@emotion/styled";

const BabenaLogo = () => {
  return <LogoContainer>🍌</LogoContainer>;
};

const LogoContainer = styled.div`
  font-size: 1.75rem;
`;

export default BabenaLogo;
